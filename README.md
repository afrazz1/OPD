# 🗺️ GroupMap - Встречайтесь с друзьями!

Современное веб-приложение для организации встреч с друзьями в реальном времени с функциями карты, чата и AI рекомендациями.

## 🎯 Основные возможности

✨ **Управление группами** - создавайте и управляйте группами встреч  
🗺️ **Карта в реальном времени** - видите позицию себя и друзей на интерактивной карте  
💬 **Чат** - общайтесь с участниками группы в реальном времени  
🤖 **AI Рекомендации** - искусственный интеллект подберет лучшие места для встреч  
🎉 **Места развлечений** - каталог интересных мест для посещения  
🎨 **Красивый интерфейс** - дружелюбный и приятный дизайн  

## 📋 Требования

- Node.js (v14+)
- npm или yarn
- Современный браузер с поддержкой геолокации

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск сервера

```bash
npm start
```

Или для развития с автоматической перезагрузкой:

```bash
npm run dev
```

Сервер запустится на `http://localhost:5000`.

### 4. Развертывание на сайте

Это приложение можно разместить на любом Node.js-хостинге (Heroku, Render, Railway, VPS и т.п.).

1. Перенесите папку проекта на сервер.
2. Установите зависимости:

```bash
npm install
```

3. Запустите приложение:

```bash
npm start
```

4. Откройте сайт по адресу вашего домена.

Клиент теперь использует текущий URL сайта для API и WebSocket, поэтому приложение будет работать не только на `localhost`, но и на удалённом хостинге.

> ВАЖНО: GitHub Pages поддерживает только статические файлы. Ваше приложение содержит backend, регистрацию, чат и Socket.IO, поэтому оно не сможет работать полностью на GitHub Pages без отдельного Node.js-сервера.

### 5. Что нужно для полноценной работы сайта

- Разместить проект на Node.js-хостинге (Heroku, Railway, Render, VPS, Fly.io и т.п.).
- Убедиться, что хостинг запускает `server.js` и задаёт `PORT` через `process.env.PORT`.
- В `public/index.html` заменить `YOUR_YANDEX_MAPS_API_KEY` на реальный ключ Яндекс.Карт.
- Доступ к сайту должен быть по HTTPS, чтобы геолокация работала корректно.

Если вы хотите, я помогу вам подготовить проект для конкретного сервиса, если укажете, какой хостинг собираетесь использовать.

### 6. Развёртывание через Railway (бесплатный сервис)

Это приложение можно разместить целиком на Railway: backend, Socket.IO и frontend будут работать в одном Node.js-сервисе.

1. Создайте бесплатный аккаунт на https://railway.app/
2. Установите Railway CLI:

```bash
npm install -g railway
```

3. Выполните в корне проекта:

```bash
railway login
railway init
```

4. Запустите развертывание:

```bash
railway up
```

5. После успешного деплоя Railway выдаст URL вашего приложения.

### 7. Развёртывание через Render (альтернатива)

Если вы хотите другой бесплатный сервис, используйте Render:

1. Зарегистрируйтесь на https://render.com/
2. Создайте новый Web Service, подключив репозиторий GitHub.
3. В поле `Build Command` укажите:

```bash
npm install
```

4. В поле `Start Command` укажите:

```bash
npm start
```

5. Render автоматически запустит приложение и выдаст URL.

> Railway и Render предлагают бесплатные планы для простых Node.js-приложений с WebSocket. Этот проект готов к развёртыванию на любом из этих сервисов.

### 7. Развёртывание на GitHub Pages

Это приложение может быть размещено на GitHub Pages как статический frontend из папки `public`.

1. Убедитесь, что в `package.json` добавлен `homepage` и скрипт `deploy`.
2. Установите зависимости:

```bash
npm install
```

3. Запустите деплой:

```bash
npm run deploy
```

4. Откройте сайт по адресу:

```text
https://afrazz1.github.io/OPD/
```

> Важно: GitHub Pages поддерживает только статические файлы. Полный функционал приложения, требующий backend и WebSocket, не будет работать на Pages без отдельного сервера.

### 3. Открытие в браузере

Откройте в браузере:
```
http://localhost:5000
```

## 🎨 Структура проекта

```
prototip/
├── server.js              # Backend Express сервер
├── package.json           # Зависимости проекта
├── .env                   # Переменные окружения
└── public/
    ├── index.html         # Главная страница
    ├── styles.css         # Стили приложения
    └── app.js             # JavaScript логика клиента
```

## 🔐 Функции авторизации

### Регистрация
1. Введите имя пользователя, email и пароль
2. Нажмите "Зарегистрироваться"
3. Вы автоматически войдете в аккаунт

### Вход
1. Введите email и пароль
2. Нажмите "Войти"

## 👥 Управление группами

### Создание группы
1. Нажмите кнопку "Создать группу"
2. Введите название и описание
3. Подтвердите создание

### Открытие группы
1. Кликните на карточку группы
2. Вы будете перенаправлены на вкладку с картой

## 🗺️ Использование карты

- Карта показывает ваше текущее местоположение (синий маркер)
- Друзья отображаются как красные маркеры
- Зум можно регулировать колесом мыши
- Кликните на маркер для дополнительной информации

## 💬 Чат

1. Перейдите на вкладку "Чат"
2. Введите сообщение в поле внизу
3. Нажмите "Отправить" или Enter
4. Сообщения отображаются в реальном времени

## 🤖 AI Рекомендации

1. Перейдите на вкладку "AI"
2. Нажмите "Получить рекомендации"
3. AI предложит лучшие места на основе ваших предпочтений
4. Нажмите на рекомендацию чтобы перейти на карте

## 🌍 Места развлечений

На вкладке "Места" вы можете:
- Просмотреть каталог интересных мест
- Видеть рейтинги и расстояния
- Кликнуть на место для навигации

## 🔧 Технологический стек

**Frontend:**
- HTML5, CSS3, Vanilla JavaScript
- Leaflet.js - для интерактивных карт
- Socket.io - для реальном времени

**Backend:**
- Node.js
- Express.js - веб-фреймворк
- Socket.io - WebSocket коммуникация
- CORS - для кроссдоменных запросов

## 🎯 Будущие улучшения

- [ ] Интеграция с реальной БД (MongoDB/PostgreSQL)
- [ ] Расширенная аутентификация (JWT, OAuth)
- [ ] Хеширование паролей (bcrypt)
- [ ] Интеграция с реальным AI сервисом
- [ ] Мобильное приложение
- [ ] Система рейтинга мест
- [ ] Сохранение истории чата
- [ ] Приватность и настройки профиля

## 🐛 Решение проблем

### Карта не загружается
- Убедитесь, что браузер имеет доступ в интернет
- Проверьте консоль браузера на ошибки

### Геолокация не работает
- Разрешите браузеру доступ к геолокации
- Используйте HTTPS или localhost

### Сервер не запускается
- Проверьте, что Node.js установлен: `node --version`
- Убедитесь, что порт 5000 свободен
- Удалите node_modules и переустановите: `rm -rf node_modules && npm install`

## 📝 Лицензия

MIT License - Свободное использование в личных и коммерческих целях

## 💡 Советы по использованию

1. **Включите геолокацию** - для точного отслеживания позиции
2. **Пригласите друзей** - добавляйте их в группы для совместных встреч
3. **Используйте AI** - получайте умные рекомендации мест
4. **Общайтесь** - используйте чат для планирования встреч

---

Создано с ❤️ для удобства общения и встреч!
