{
  "name": "group-meetup-app",
  "version": "1.0.0",
  "description": "App for group meetups with maps, chat, and AI recommendations",
  "homepage": "https://afrazz1.github.io/OPD",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "deploy": "gh-pages -d public",
    "deploy:firebase": "npx firebase deploy",
    "deploy:full": "gcloud run deploy opd-backend --source . --region us-central1 --platform managed --allow-unauthenticated --port 5000 && firebase deploy --only hosting"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "socket.io": "^4.5.4"
  },
  "devDependencies": {
    "firebase-tools": "^12.0.0",
    "gh-pages": "^5.0.0",
    "nodemon": "^2.0.20"
  }
}
